const Artikal = ({ title, description, description2 }) => {
  return (
    <div>
      <div>
        <h1>{title}</h1>
        <h4>{description}</h4>
        <p>{description2}</p>
      </div>
    </div>
  );
};
export default Artikal;
